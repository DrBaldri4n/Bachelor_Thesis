\subsection{Data Preprocessing steps}
As already mentioned for the  \texttt{CICEVSE2024} dataset it was just need to remove the four rows of NaN data to have correct useable data for the first step.\\
For the gerenated dataset from our environment I needed at first do have as much as possbile data in the benign status thereforce I made an Prometheus PromQL request to get the monitored data from the night where the environment was not used for other test. The script for this query you can find at listing4.\\
Than I started with the attacks to monitor the behavior from the hcp and kernel events in our environment if it is under attack. You can find this script under listening5\\

%dann attack namen eingefügt

%alle skripte einen kurzen ausschnitt noch erklären

%dann kann ich mit key feature selection starten